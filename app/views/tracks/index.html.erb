<%= link_to "Upload a New Song", new_track_path %>

<h2>Stream A Song</h2>
<section id='audio'>

</section>
<ol>
  <% @songs.each do |song| %>
    <li>
      <%= song.key %>
    </li>
  <% end %>
</ol>
