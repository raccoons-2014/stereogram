<%= form_for current_user.follows.new(:followed_id => @user.id),
                                       remote: true)  do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <button><%= f.submit "Follow "%></button>
<% end %>
