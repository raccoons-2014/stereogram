<ul>
  <div class="following">
  <% @user.following.each do |user| %>
    <li>
      <%= link_to image_tag(user.profile_img_url), user_path(user) %>
      <%= "#{user.full_name}" %>
    </li>
  <% end %>
  </div>
</ul>
